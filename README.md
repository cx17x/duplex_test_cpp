# Duplex Virtual Serial Test

Описание задания и полный отчёт находятся в `report/README.md`.

Быстрый старт:
1. `g++ -std=c++17 src/*.cpp -pthread -o virtual_serial`
2. `./virtual_serial`

См. `report/README.md` для постановки, архитектуры и результатов.
